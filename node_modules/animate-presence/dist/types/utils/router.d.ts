import { Path } from './path-to-regex';
/**
 * Public API for matching a URL pathname to a path pattern.
 */
export declare const matchPath: (pathname: string, options?: MatchOptions) => MatchResults;
export interface LocationSegments {
    pathname: string;
    query: {
        [key: string]: any;
    };
    key: string;
    scrollPosition?: [number, number];
    search?: string;
    hash?: string;
    state?: any;
}
export interface MatchResults {
    path: string;
    url: string;
    isExact: boolean;
    params: {
        [key: string]: string;
    };
}
export interface MatchOptions {
    path?: Path;
    exact?: boolean;
    strict?: boolean;
}
