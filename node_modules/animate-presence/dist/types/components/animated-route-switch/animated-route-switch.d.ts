import { QueueApi } from '@stencil/core/dist/declarations';
import { MatchResults, LocationSegments } from '../../utils/router';
interface Child {
    el: any;
    match: MatchResults | null;
}
export declare class AnimatedRouteSwitch {
    el: HTMLElement;
    /** @internal */
    queue: QueueApi;
    /** @internal */
    group: string;
    /** @internal */
    routeViewsUpdated?: (options: any) => void;
    scrollTopOffset?: number;
    location: LocationSegments;
    activeIndex?: number;
    prevIndex?: number;
    subscribers: Child[];
    componentWillLoad(): void;
    regenerateSubscribers(newLocation: LocationSegments): Promise<void>;
    render(): any;
}
export {};
